# Backend environment example for AI Copilot (Gemini)
# Copy this file to .env and fill in values as needed.

# Google Gemini API Key (Required for real answers)
# Get one from https://aistudio.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=

# Default model; you can change to a different available Gemini model
GEMINI_MODEL=gemini-1.5-pro

# Frontend origin allowed by CORS (adjust if your frontend runs elsewhere)
FRONTEND_ORIGIN=http://localhost:3000

# Backend port for uvicorn (also used by docs/README examples)
BACKEND_PORT=3001
